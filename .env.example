# App bind settings
HOST=0.0.0.0
PORT=3000

# SQLite path. Use /data/spyfall.db in Docker, or a persistent VPS path.
DB_PATH=/data/spyfall.db

# Optional Google sign-in. Leave empty to run without sign-in.
# GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
